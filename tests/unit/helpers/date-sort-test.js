import { dateSort } from 'blog/helpers/date-sort';
import { module, test } from 'qunit';

module('Unit | Helper | date sort');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateSort([42]);
  assert.ok(result);
});
